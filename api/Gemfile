source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "3.3.0"

gem "rails", "~> 7.1.0"
gem "pg", "~> 1.5"
gem "puma", "~> 6.4"
gem "rack-cors"
gem "bootsnap", require: false

# Auth
gem "devise"
gem "devise-jwt"
gem "pundit"
gem "bcrypt", "~> 3.1"  # explicit dep for PersonalAccessToken hashing

# Serialization
gem "jsonapi-serializer"

# Background jobs — pin connection_pool to avoid Ruby 3.3 syntax error
gem "connection_pool", "~> 2.4"
gem "sidekiq", "~> 7.2.0"

# Storage — Cloudflare R2 is S3-compatible
gem "aws-sdk-s3", "~> 1.218"

# Image processing
gem "ruby-vips", "~> 2.2"

# Audio processing
gem "streamio-ffmpeg", "~> 3.0"

# Payments
gem "stripe", "~> 19.0"

# RSS / XML
gem "builder"

# HTTP client (AI integrations Phase 3)
gem "faraday"
gem "faraday-retry"

# AI — Whisper transcription
gem "ruby-openai", "~> 7.0"

# Pagination
gem "pagy"

gem "dotenv-rails", groups: [:development, :test]
gem "tzinfo-data", platforms: %i[windows jruby]

group :development, :test do
  gem "rspec-rails", "~> 7.1"
  gem "factory_bot_rails"
  gem "faker"
  gem "shoulda-matchers"
  gem "database_cleaner-active_record"
  gem "rubocop-rails-omakase", require: false
end

group :development do
  gem "listen"
  gem "letter_opener"
end

<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="row form-inputs paddingbottom">
    <div class="col-md-8">
      <%= f.input :first_name %>
      <%= f.input :name %>
      <%= f.input :email %>
      <%= f.input :status %>
      <%= f.input :address %>
      <div class="row">
        <div class="col">
          <%= f.input :city %>
        </div>
        <div class="col">
          <%= f.input :state %>
        </div>
        <div class="col">
          <%= f.input :zip %>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <label class="form-control-label string optional" for="user_avatar">Avatar</label>

      <% if @user.avatar.attached? %>
        <%= image_tag @user.avatar, width: "100%", class: "paddingbottom" %>
      <% else %>
        <%= image_tag "default_user_avatar.jpg",  width: "100%", class: "paddingbottom" %>
      <% end %>

      <%= f.file_field :avatar, multiple: false, direct_upload: true %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary btn-block" %>
  </div>
<% end %>
